[package]
name = "coregraph-core"
version.workspace = true
edition.workspace = true
description = "Shared types for CoreGraph (graph, symbol, edge, span, file-state hashing)"

[dependencies]
serde = { workspace = true }
xxhash-rust = { workspace = true }

[dev-dependencies]
serde_json = "1"
